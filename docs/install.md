# Installation

Install git by going to [https://git-scm.com/](https://git-scm.com/)

[git install page](https://git-scm.com/install/mac)

Often git is already installed on Linux. Check if you have it by opening a terminal and doing a `git -v`.
